import {
  Zap,
  ZapOff,
  Search,
  X,
  ChevronDown,
  ChevronRight,
  ChevronLeft,
  Plus,
  AlertTriangle,
  Users,
  Hourglass,
  List,
  Clock,
  MapPin,
  CheckCircle2,
  Coins,
} from "lucide-react";

// Thin re-exports under the app's existing names, so call sites don't change.
// Lucide's defaults (24x24 grid, currentColor stroke, 2px round-cap strokes)
// already match the hand-rolled set this replaces.
export const BoltIcon = Zap;
export const BoltOffIcon = ZapOff;
export const SearchIcon = Search;
export const XIcon = X;
export const ChevronDownIcon = ChevronDown;
export const ChevronRightIcon = ChevronRight;
export const ChevronLeftIcon = ChevronLeft;
export const PlusIcon = Plus;
export const AlertIcon = AlertTriangle;
export const UsersIcon = Users;
export const HourglassIcon = Hourglass;
export const ListIcon = List;
export const ClockIcon = Clock;
export const MapPinIcon = MapPin;
export const CheckCircleIcon = CheckCircle2;
export const CoinIcon = Coins;
